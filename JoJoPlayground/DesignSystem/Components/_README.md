# Components

Reusable design system components live here. Each file uses the `DS` prefix.

- File naming: `DS{ComponentName}.swift`
- Use only `DSColors`, `DSTypography`, `DSSpacing` tokens
- Include `#Preview` at the bottom of every component file
- Add each component to `SPEC.md`
