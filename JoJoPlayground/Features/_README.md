# Features

Each flow lives in its own folder here. See **FLOW_TEMPLATE.md** in the project root for the complete step-by-step guide.

```
Features/{FlowName}/
├── {FlowName}Flow.swift          # Flow coordinator
├── {FlowName}ViewModel.swift     # ViewModel + step enum
└── Views/
    ├── FirstScreenView.swift
    └── ...
```
